# ============================================================
# 仓库根 = banzhuren/ 这个文件夹本身（GitHub 上仓库名也叫 banzhuren）
# 也就是说：本文件中没有被忽略的东西，都是要上传到 GitHub 的。
# 下面这些是「本机才有、不该上传」的类型。
# ============================================================

# --- 依赖（体积大，用 npm install 重建）---
node_modules/

# --- 编辑器 / 操作系统垃圾 ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
npm-debug.log*

# --- 本机 IDE 工作目录（现在位于仓库之外，留着以防误放进来）---
.workbuddy/

# --- 开发侧目录（已移到仓库同级的 dev/：文档 / 测试 / 依赖）---
# 留着这几条是为了防止它们被误放回仓库；真要放回来，请同时删掉这里的规则。
docs/
tests/

# --- 一次性临时脚本（用完即删，含仓库根下以 _ 开头者）---
_*.cjs
_*.mjs
